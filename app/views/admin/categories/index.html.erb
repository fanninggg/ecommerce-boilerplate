<p>Admin</p>
<% @categories.each do |cat| %>
  <h3><%= link_to cat.name, admin_category_path(cat) %></h3>
<% end %>

<%= link_to "Add a category", new_admin_category_path, class: "btn btn-primary" %>
